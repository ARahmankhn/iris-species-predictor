import joblib

# Load the model bundle from the pkl file
model_bundle = joblib.load("/mnt/data/deployment.pkl")

# Print the keys to see if the model is structured correctly
print(model_bundle.keys())  # Should print dict_keys(['model', 'feature_names', 'target_names'])

# Optionally, print out the structure of each part to inspect them
print(model_bundle['model'])  # Should print out model info
print(model_bundle['feature_names'])  # Should print the list of feature names
print(model_bundle['target_names'])  # Should print the list of target names
